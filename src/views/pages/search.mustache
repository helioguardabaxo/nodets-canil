{{>partials/header}}

{{#banner}}
<section class="banner" style="background-image: url('images/{{ banner.background }}')">{{ title }}</section>
<h2>{{ title }} disponíveis para adoção</h2>
{{/banner}}

<div class="container list">
    {{#list}}
        <div class="item">
            <img src="images/{{image}}" class="item--image">
            <div class="item--name">{{name}}</div>
            <div class="item--color">{{color}}</div>
            <div class="item--genre">{{sex}}</div>
        </div>
    {{/list}}
</div>

{{^list}}
<div class="container no-list">
    <h2>Nenhum animal encotrado com o nome buscado</h2>
</div>
{{/list}}

{{>partials/footer}}